[Unit]
Description=rait
After=network.target

[Service]
Type=oneshot
RemainAfterExit=true
ExecStart=/usr/local/bin/rait up -c /etc/rait/rait.conf -d /etc/rait/peers
ExecStop=/usr/local/bin/rait down -c /etc/rait/rait.conf

[Install]
WantedBy=multi-user.target

